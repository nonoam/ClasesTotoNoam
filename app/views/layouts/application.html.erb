<!DOCTYPE html>
<html lang="es">
  <head>
    <title>TutorSchedule - Clases Particulares de Calidad</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Agenda tus clases particulares de Cálculo, Programación y Excel con tutores expertos">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <div class="container">
        <div class="nav-brand">
          <%= link_to "TutorSchedule", root_path, class: "logo" %>
        </div>
        <ul class="nav-menu">
          <li><%= link_to "Inicio", root_path %></li>
          <li><%= link_to "Horarios", schedules_availability_path %></li>
          <li><%= link_to "Nuestra Experiencia", experience_path %></li>
          <li><%= link_to "Precios", pricing_path %></li>
          <li><%= link_to "Reservar Clase", new_booking_path, class: "btn-primary" %></li>
        </ul>
      </div>
    </nav>
    
    <% if flash.any? %>
      <div class="flash-messages">
        <% flash.each do |type, message| %>
          <div class="alert alert-<%= type %>" data-turbo-temporary>
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
    
    <footer class="footer">
      <div class="container">
        <p>&copy; 2024 TutorSchedule. Todos los derechos reservados.</p>
        <div class="social-links">
          <a href="#">Instagram</a>
          <a href="#">LinkedIn</a>
          <a href="#">WhatsApp</a>
        </div>
      </div>
    </footer>
  </body>
</html>